// Manually bumped on every change so Metro/dev-client testers can tell, at a glance,
// whether they're on the latest JS bundle instead of guessing (Ryan 2026-07-19).
// Bump this string whenever you push a change you want testers to be able to confirm.
export const BUILD_TAG = '2026-07-28 · v1.0.0 — messaging inboxes (buyer + seller)';
